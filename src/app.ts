import { UsernameGenerator } from "./helpers/username-generator/username-generator";
import './assets/scss/app.scss';

document.addEventListener('DOMContentLoaded', () => {
  new UsernameGenerator();
});
